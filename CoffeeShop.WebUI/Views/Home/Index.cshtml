@{
    ViewBag.Title = "Home";
}

<div class="row">
    <div class="col-md-12">
        <section class="top">
            <div class="caption">
                <h3>Welcome to the CoffeeShop</h3>
                <h4>
                    <span class="rsep"></span>
                    Best coffee around!
                    <span class="lsep"></span>
                </h4>
            </div>
        </section>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="col-md-offset-5">
            @Html.ActionLink("Sofia", "Index", "Store", new { city = "Sofia" }, new { @class = "btn btn-default", role = "button" })
        </div>
    </div>
    <div class="col-md-6">
        <div class="col-md-offset-5">
            @Html.ActionLink("Plovdiv", "Index", "Store", new { city = "Plovdiv" }, new { @class = "btn btn-default", role = "button" })
        </div>
    </div>
</div>

@section Footer {
    This is the <strong>footer</strong>.
}