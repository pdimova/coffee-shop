@using CoffeeShop.WebUI.ViewModels.Store
@model FinalOrderViewModel

@{
    ViewBag.Title = "Successful Order";
}

<div class="row">
    <div class="col-md-6">
        <h3>Bravo!</h3>

        @Html.DisplayFor(m => m.FullDescription) - @Html.DisplayFor(m => m.Price)

        <br />
        <br />

        @Html.ActionLink("Add to cart", "AddToCart", "ShoppingCart", "", new { @class = "btn btn-warning", role = "button" })
    </div>
</div>
